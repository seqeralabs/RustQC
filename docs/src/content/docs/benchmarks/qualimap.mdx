---
title: Qualimap Benchmarks
description: Benchmark comparison of RustQC's Qualimap rnaseq output against the original Qualimap Java implementation.
---

export const base = import.meta.env.BASE_URL;

## Performance

RustQC's [Qualimap analysis](/outputs/qualimap/) runs in the same single-pass
BAM scan as all other tools, eliminating the separate name-sort and Qualimap
run required by the original Java implementation.

### Large dataset (~186M reads, GM12878)

| Tool                        | Runtime        | Peak RSS | Notes                                                          |
| --------------------------- | -------------- | -------- | -------------------------------------------------------------- |
| `samtools sort -n`          | 12m 16s        | 5.2 GB   | Name-sort required by Qualimap Java (not needed by RustQC)     |
| Qualimap rnaseq (Java)      | 49m 58s        | 4.9 GB   | Separate BAM pass, single-threaded, requires name-sorted input |
| **Total upstream pipeline** | **~1h 2m 14s** |          | Name-sort + Qualimap combined                                  |
| RustQC (all tools)          | 14m 54s        | 11.4 GB  | Single pass, all analyses combined, coordinate-sorted input    |

> **Note:** RustQC runtime shown is for all tools combined in a single pass. See [Combined benchmarks](/benchmarks/combined/) for a full breakdown.

### Small dataset (~52K reads, chr6)

| Tool                   | Runtime | Peak RSS | Notes                                         |
| ---------------------- | ------- | -------- | --------------------------------------------- |
| Qualimap rnaseq (Java) | 5.0s    | 704.6 MB | Separate BAM pass, requires name-sorted input |
| RustQC (all tools)     | 25.9s   | 182.1 MB | Single pass, all analyses combined            |

On the small dataset, RustQC uses 182 MB of memory vs Qualimap's 705 MB.

## Output comparison

RustQC produces output effectively identical to Qualimap Java.
Both tools were run on the same BAM file with matching parameters
(`--stranded 2` / `strand-specific-reverse`, same GENCODE GTF). Results below
are from the AWS benchmark run (2026-03-09).

### Small dataset (~52K reads, chr6)

#### Reads alignment

| Metric                | Qualimap (Java) | RustQC | Match? |
| --------------------- | --------------- | ------ | ------ |
| reads aligned (left)  | 24,797          | 24,797 | ✅     |
| reads aligned (right) | 24,776          | 24,776 | ✅     |
| read pairs aligned    | 24,773          | 24,773 | ✅     |
| total alignments      | 52,839          | 52,839 | ✅     |
| secondary alignments  | 3,266           | 3,266  | ✅     |
| non-unique alignments | 5,496           | 5,496  | ✅     |
| aligned to genes      | 31,654          | 31,654 | ✅     |
| ambiguous alignments  | 1,054           | 1,054  | ✅     |
| no feature assigned   | 14,635          | 14,635 | ✅     |
| not aligned           | 0               | 0      | ✅     |

#### Reads genomic origin

| Metric              | Qualimap (Java) | RustQC | Match? |
| ------------------- | --------------- | ------ | ------ |
| exonic fraction     | 68.38%          | 68.38% | ✅     |
| intronic fraction   | 29.57%          | 29.57% | ✅     |
| intergenic fraction | 2.04%           | 2.04%  | ✅     |

#### Transcript coverage profile

| Metric     | Qualimap (Java) | RustQC | Match?   |
| ---------- | --------------- | ------ | -------- |
| 5' bias    | 0.71            | 0.71   | ✅       |
| 3' bias    | 0.57            | 0.57   | ✅       |
| 5'→3' bias | 1.30            | 1.19   | ⚠️ minor |

The 5'→3' bias difference (1.30 vs 1.19) is the only deviation on this
dataset. It arises because upstream Qualimap requires a name-sorted BAM while
RustQC uses the standard coordinate-sorted BAM. All other values are identical.

#### Junction analysis

| Metric             | Qualimap (Java) | RustQC | Match? |
| ------------------ | --------------- | ------ | ------ |
| reads at junctions | 28,125          | 28,125 | ✅     |

### Large dataset (~186M reads, GM12878)

#### Reads alignment

| Metric                | Qualimap (Java) | RustQC      | Match? |
| --------------------- | --------------- | ----------- | ------ |
| reads aligned (left)  | 86,909,417      | 86,909,417  | ✅     |
| reads aligned (right) | 86,888,892      | 86,888,892  | ✅     |
| read pairs aligned    | 86,778,882      | 86,778,882  | ✅     |
| total alignments      | 189,114,475     | 189,114,475 | ✅     |
| secondary alignments  | 15,316,166      | 15,316,166  | ✅     |
| non-unique alignments | 26,451,119      | 26,451,119  | ✅     |
| aligned to genes      | 122,500,822     | 122,500,822 | ✅     |
| ambiguous alignments  | 2,422,409       | 2,422,409   | ✅     |
| no feature assigned   | 37,740,125      | 37,740,125  | ✅     |
| not aligned           | 12,490,977      | 12,490,977  | ✅     |

#### Reads genomic origin

| Metric              | Qualimap (Java) | RustQC | Match? |
| ------------------- | --------------- | ------ | ------ |
| exonic fraction     | 76.45%          | 76.45% | ✅     |
| intronic fraction   | 18.37%          | 18.37% | ✅     |
| intergenic fraction | 5.19%           | 5.19%  | ✅     |
| overlapping exon    | 4.20%           | 4.20%  | ✅     |

#### Transcript coverage profile

| Metric     | Qualimap (Java) | RustQC | Match? |
| ---------- | --------------- | ------ | ------ |
| 5' bias    | 0.87            | 0.87   | ✅     |
| 3' bias    | 0.77            | 0.77   | ✅     |
| 5'→3' bias | 1.11            | 1.11   | ✅     |

#### Junction analysis

| Metric             | Qualimap (Java) | RustQC     | Match? |
| ------------------ | --------------- | ---------- | ------ |
| reads at junctions | 56,119,719      | 56,119,719 | ✅     |

Junction motif type percentages match with rounding differences ≤0.05
percentage points. The underlying read counts are identical.

RustQC produces a 100-bin coverage profile along normalized gene body positions
(5' to 3'), matching the format produced by Qualimap. Three tiers are generated:
total (all genes), high (top 500 by expression), and low (bottom 500).

The high-expression tier coverage profile is an exact numerical match with
Qualimap Java. The total (all-genes) tier shows negligible floating-point
differences (&lt;0.03% relative) due to coverage aggregation order.

### HTML reports

Both tools produce an HTML report with embedded charts and summary tables.
View the full reports from the large benchmark run:

- <a
    href={`${base}benchmarks/large/qualimap/Qualimap/qualimapReport.html`}
    target="_blank"
  >
    Qualimap (Java) HTML report
  </a>
- <a
    href={`${base}benchmarks/large/qualimap/RustQC/qualimapReport.html`}
    target="_blank"
  >
    RustQC HTML report
  </a>

### Chart comparison

RustQC generates the same set of charts as Qualimap's HTML report. Below are
side-by-side comparisons from the large benchmark dataset (GM12878).

#### Coverage Profile Along Genes (Total)

<div
  style={{
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: "1rem",
    margin: "1rem 0",
  }}
>
  <figure style={{ margin: 0, textAlign: "center" }}>
    <div
      style={{ background: "white", borderRadius: "8px", padding: "0.25rem" }}
    >
      <img
        src={`${base}benchmarks/large/qualimap/Qualimap/Coverage Profile Along Genes (Total).png`}
        alt="Qualimap coverage profile"
        style={{ width: "100%", borderRadius: "8px" }}
      />
    </div>
    <figcaption>Qualimap (Java)</figcaption>
  </figure>
  <figure style={{ margin: 0, textAlign: "center" }}>
    <div
      style={{ background: "white", borderRadius: "8px", padding: "0.25rem" }}
    >
      <img
        src={`${base}benchmarks/large/qualimap/RustQC/Coverage Profile Along Genes (Total).png`}
        alt="RustQC coverage profile"
        style={{ width: "100%", borderRadius: "8px" }}
      />
    </div>
    <figcaption>RustQC</figcaption>
  </figure>
</div>

#### Coverage Histogram (0-50X)

<div
  style={{
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: "1rem",
    margin: "1rem 0",
  }}
>
  <figure style={{ margin: 0, textAlign: "center" }}>
    <div
      style={{ background: "white", borderRadius: "8px", padding: "0.25rem" }}
    >
      <img
        src={`${base}benchmarks/large/qualimap/Qualimap/Transcript coverage histogram.png`}
        alt="Qualimap coverage histogram"
        style={{ width: "100%", borderRadius: "8px" }}
      />
    </div>
    <figcaption>Qualimap (Java)</figcaption>
  </figure>
  <figure style={{ margin: 0, textAlign: "center" }}>
    <div
      style={{ background: "white", borderRadius: "8px", padding: "0.25rem" }}
    >
      <img
        src={`${base}benchmarks/large/qualimap/RustQC/Transcript coverage histogram.png`}
        alt="RustQC coverage histogram"
        style={{ width: "100%", borderRadius: "8px" }}
      />
    </div>
    <figcaption>RustQC</figcaption>
  </figure>
</div>

#### Reads Genomic Origin

<div
  style={{
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: "1rem",
    margin: "1rem 0",
  }}
>
  <figure style={{ margin: 0, textAlign: "center" }}>
    <div
      style={{ background: "white", borderRadius: "8px", padding: "0.25rem" }}
    >
      <img
        src={`${base}benchmarks/large/qualimap/Qualimap/Reads Genomic Origin.png`}
        alt="Qualimap reads genomic origin"
        style={{ width: "100%", borderRadius: "8px" }}
      />
    </div>
    <figcaption>Qualimap (Java)</figcaption>
  </figure>
  <figure style={{ margin: 0, textAlign: "center" }}>
    <div
      style={{ background: "white", borderRadius: "8px", padding: "0.25rem" }}
    >
      <img
        src={`${base}benchmarks/large/qualimap/RustQC/Reads Genomic Origin.png`}
        alt="RustQC reads genomic origin"
        style={{ width: "100%", borderRadius: "8px" }}
      />
    </div>
    <figcaption>RustQC</figcaption>
  </figure>
</div>

#### Junction Analysis

<div
  style={{
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: "1rem",
    margin: "1rem 0",
  }}
>
  <figure style={{ margin: 0, textAlign: "center" }}>
    <div
      style={{ background: "white", borderRadius: "8px", padding: "0.25rem" }}
    >
      <img
        src={`${base}benchmarks/large/qualimap/Qualimap/Junction Analysis.png`}
        alt="Qualimap junction analysis"
        style={{ width: "100%", borderRadius: "8px" }}
      />
    </div>
    <figcaption>Qualimap (Java)</figcaption>
  </figure>
  <figure style={{ margin: 0, textAlign: "center" }}>
    <div
      style={{ background: "white", borderRadius: "8px", padding: "0.25rem" }}
    >
      <img
        src={`${base}benchmarks/large/qualimap/RustQC/Junction Analysis.png`}
        alt="RustQC junction analysis"
        style={{ width: "100%", borderRadius: "8px" }}
      />
    </div>
    <figcaption>RustQC</figcaption>
  </figure>
</div>

### QC results file

The `rnaseq_qc_results.txt` file is formatted identically to Qualimap's output
and is directly parseable by MultiQC's Qualimap rnaseq module. See the
[Qualimap output page](/outputs/qualimap/#qc-results) for full field
descriptions and implementation details.

## Benchmark conditions

- **Small dataset**: `test` sample, ~52K reads, chr6 only
- **Large dataset**: GM12878 ENCODE RNA-Seq, paired-end, ~186M reads
- **Run date**: 2026-03-09 (AWS cloud execution)
- **Annotation**: GENCODE v47 GTF (filtered to standard chromosomes, used for both tools)
- **Strandedness**: strand-specific-reverse (`--stranded 2` / `-p strand-specific-reverse`)
- **Qualimap version**: 2.3 (Java implementation, name-sorted BAM input)
- **RustQC**: coordinate-sorted BAM input, 174.2% CPU utilization on large dataset
- **Infrastructure**: AWS cloud (via Nextflow/Seqera Platform)
